// Fill these in after completing the Google Cloud Console setup in README.md.
// None of these are secret — they identify your app to Google, they don't
// authenticate anything by themselves.
export const CONFIG = {
  CLIENT_ID: 'REPLACE_WITH_YOUR_OAUTH_CLIENT_ID.apps.googleusercontent.com',
  API_KEY: 'REPLACE_WITH_YOUR_PICKER_API_KEY',
  APP_ID: 'REPLACE_WITH_YOUR_GOOGLE_CLOUD_PROJECT_NUMBER',
  SCOPES: 'https://www.googleapis.com/auth/drive.file',
};
